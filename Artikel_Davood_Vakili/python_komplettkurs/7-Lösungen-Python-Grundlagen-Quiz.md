1. `my_list = []`  
2. صفر  
3. `if not my_list:` یا `len(my_list) == 0`  
4. `append()` یا `insert()`  
5. بله، ولی بدون تکرار  
6. خیر  
7. بله  
8. بله  
9. بله  
10. بله  
11. `list()`  
12. بله، ولی توصیه نمی‌شود  
13. `my_list[:] = [1, 2]`  
14. بله  
15. False در شرط  

16. `abs()`  
17. `break`  
18. `int()`  
19. `type()`  
20. `if`  
21. `sort()`  
22. `dict.keys()`  
23. `'w'`  
24. `dict.update()`  
25. `in`  
26. `random.randint(1, 10)`  
27. `import`  
28. `min()`  
29. `for` یا `while`  
30. `class`  
31. `sort(reverse=True)`  
32. `global`  
33. `pop()`  
34. `max()`  
35. `len()`  
36. `str` یا `bool`  
37. `for item in list`  
38. `list.index()`  
39. `sum()`  
40. `len(dict)`  
41. `file.close()`  
42. `lambda`  
43. `dict.items()`  
44. `del dict['key']`  
45. `type()`  
46. `continue`  
47. `pow()` یا `**`  
48. `list.insert(0, element)`  
49. `list.extend()`  
50. `bool`  
51. `str.upper()`  
52. `if`  
53. `try...except`  
54. `file.write()` با `'a'`  
55. `list.clear()`  
56. `slice` یا `list[start:end]`  
57. `list.copy()`  
58. `float()`  
59. `str.strip()`  
60. `for`  
61. `range()`  
62. `%`  
63. `dict.get(key)`  
64. `def`  
65. `//`  
66. `'separator'.join(list)`  
67. `list.count(element)`  
68. `bool()`  
69. `str.lower()`  
70. `list.extend([...])`  
71. حروف بزرگ یا `Final`  
72. `import`  
73. `list`  
74. `list.reverse()`  
75. `'substring' in 'string'`  
76. `np.array(list)`  
77. `list.remove(element)`  
78. `copy.deepcopy(dict)`  
79. `a % b`  
80. `'string' * 3`
