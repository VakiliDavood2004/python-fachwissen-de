#### 🇩🇪 Deutsch
Diese Datei enthält die Lösungen zum „Python-Grundlagen-Quiz“, das in 6-Python-Grundlagen-Quiz.md zu finden ist. Wir empfehlen, die Fragen zunächst selbstständig zu beantworten und anschließend diese Datei zur Kontrolle und Vertiefung zu nutzen. Alle Lösungen sind präzise, kompakt und direkt in Python ausführbar. Viel Erfolg! 🧩
---
#### 🇬🇧 English
This file contains the answers to the "Python Fundamentals Quiz" found in 6-Python-Grundlagen-Quiz.md. We recommend solving the questions first without looking at the answers, then using this file to check your work and deepen your understanding. All answers are concise, accurate, and executable in Python. Good luck! 🧠
---
#### 🇮🇷 فارسی
این فایل شامل پاسخ‌های آزمون «مبانی پایتون» است که در فایل 6-Python-Grundlagen-Quiz.md قرار دارد. توصیه می‌کنیم ابتدا سوال‌ها را بدون مراجعه به پاسخ‌نامه حل کنید، سپس برای بررسی و یادگیری بیشتر به این فایل مراجعه نمایید. پاسخ‌ها به صورت خلاصه، دقیق و قابل اجرا در پایتون نوشته شده‌اند. موفق باشید! ✅

---

## 🇩🇪 Lösungsblatt

1. `my_list = []`  
2. Null  
3. `if not my_list:` oder `len(my_list) == 0`  
4. `append()` oder `insert()`  
5. Ja, aber ohne Wiederholung  
6. Nein  
7. Ja  
8. Ja  
9. Ja  
10. Ja  
11. `list()`  
12. Ja, aber nicht empfohlen  
13. `my_list[:] = [1, 2]`  
14. Ja  
15. False in der Bedingung  

16. `abs()`  
17. `break`  
18. `int()`  
19. `type()`  
20. `if`  
21. `sort()`  
22. `dict.keys()`  
23. `'w'`  
24. `dict.update()`  
25. `in`  
26. `random.randint(1, 10)`  
27. `import`  
28. `min()`  
29. `for` oder `while`  
30. `class`  
31. `sort(reverse=True)`  
32. `global`  
33. `pop()`  
34. `max()`  
35. `len()`  
36. `str` oder `bool`  
37. `for item in list`  
38. `list.index()`  
39. `sum()`  
40. `len(dict)`  
41. `file.close()`  
42. `lambda`  
43. `dict.items()`  
44. `del dict['key']`  
45. `type()`  
46. `continue`  
47. `pow()` oder `**`  
48. `list.insert(0, element)`  
49. `list.extend()`  
50. `bool`  
51. `str.upper()`  
52. `if`  
53. `try...except`  
54. `file.write()` mit `'a'`  
55. `list.clear()`  
56. `slice` oder `list[start:end]`  
57. `list.copy()`  
58. `float()`  
59. `str.strip()`  
60. `for`  
61. `range()`  
62. `%`  
63. `dict.get(key)`  
64. `def`  
65. `//`  
66. `'separator'.join(list)`  
67. `list.count(element)`  
68. `bool()`  
69. `str.lower()`  
70. `list.extend([...])`  
71. Großbuchstaben oder `Final`  
72. `import`  
73. `list`  
74. `list.reverse()`  
75. `'substring' in 'string'`  
76. `np.array(list)`  
77. `list.remove(element)`  
78. `copy.deepcopy(dict)`  
79. `a % b`  
80. `'string' * 3`

---
---

## 🇬🇧 Answer Key

1. `my_list = []`  
2. Zero  
3. `if not my_list:` or `len(my_list) == 0`  
4. `append()` or `insert()`  
5. Yes, but without repetition  
6. No  
7. Yes  
8. Yes  
9. Yes  
10. Yes  
11. `list()`  
12. Yes, but not recommended  
13. `my_list[:] = [1, 2]`  
14. Yes  
15. False in condition  

16. `abs()`  
17. `break`  
18. `int()`  
19. `type()`  
20. `if`  
21. `sort()`  
22. `dict.keys()`  
23. `'w'`  
24. `dict.update()`  
25. `in`  
26. `random.randint(1, 10)`  
27. `import`  
28. `min()`  
29. `for` or `while`  
30. `class`  
31. `sort(reverse=True)`  
32. `global`  
33. `pop()`  
34. `max()`  
35. `len()`  
36. `str` or `bool`  
37. `for item in list`  
38. `list.index()`  
39. `sum()`  
40. `len(dict)`  
41. `file.close()`  
42. `lambda`  
43. `dict.items()`  
44. `del dict['key']`  
45. `type()`  
46. `continue`  
47. `pow()` or `**`  
48. `list.insert(0, element)`  
49. `list.extend()`  
50. `bool`  
51. `str.upper()`  
52. `if`  
53. `try...except`  
54. `file.write()` with `'a'`  
55. `list.clear()`  
56. `slice` or `list[start:end]`  
57. `list.copy()`  
58. `float()`  
59. `str.strip()`  
60. `for`  
61. `range()`  
62. `%`  
63. `dict.get(key)`  
64. `def`  
65. `//`  
66. `'separator'.join(list)`  
67. `list.count(element)`  
68. `bool()`  
69. `str.lower()`  
70. `list.extend([...])`  
71. Capital letters or `Final`  
72. `import`  
73. `list`  
74. `list.reverse()`  
75. `'substring' in 'string'`  
76. `np.array(list)`  
77. `list.remove(element)`  
78. `copy.deepcopy(dict)`  
79. `a % b`  
80. `'string' * 3`

---
---

## 🇮🇷 پاسخ‌نامه 

1. `my_list = []`  
2. صفر  
3. `if not my_list:` یا `len(my_list) == 0`  
4. `append()` یا `insert()`  
5. بله، ولی بدون تکرار  
6. خیر  
7. بله  
8. بله  
9. بله  
10. بله  
11. `list()`  
12. بله، ولی توصیه نمی‌شود  
13. `my_list[:] = [1, 2]`  
14. بله  
15. False در شرط  

16. `abs()`  
17. `break`  
18. `int()`  
19. `type()`  
20. `if`  
21. `sort()`  
22. `dict.keys()`  
23. `'w'`  
24. `dict.update()`  
25. `in`  
26. `random.randint(1, 10)`  
27. `import`  
28. `min()`  
29. `for` یا `while`  
30. `class`  
31. `sort(reverse=True)`  
32. `global`  
33. `pop()`  
34. `max()`  
35. `len()`  
36. `str` یا `bool`  
37. `for item in list`  
38. `list.index()`  
39. `sum()`  
40. `len(dict)`  
41. `file.close()`  
42. `lambda`  
43. `dict.items()`  
44. `del dict['key']`  
45. `type()`  
46. `continue`  
47. `pow()` یا `**`  
48. `list.insert(0, element)`  
49. `list.extend()`  
50. `bool`  
51. `str.upper()`  
52. `if`  
53. `try...except`  
54. `file.write()` با `'a'`  
55. `list.clear()`  
56. `slice` یا `list[start:end]`  
57. `list.copy()`  
58. `float()`  
59. `str.strip()`  
60. `for`  
61. `range()`  
62. `%`  
63. `dict.get(key)`  
64. `def`  
65. `//`  
66. `'separator'.join(list)`  
67. `list.count(element)`  
68. `bool()`  
69. `str.lower()`  
70. `list.extend([...])`  
71. حروف بزرگ یا `Final`  
72. `import`  
73. `list`  
74. `list.reverse()`  
75. `'substring' in 'string'`  
76. `np.array(list)`  
77. `list.remove(element)`  
78. `copy.deepcopy(dict)`  
79. `a % b`  
80. `'string' * 3`
